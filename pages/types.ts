export type RandPerson = {
  firstName: string;
  lastName: string;
  email: string;
}

export type Candidate = {
    vacancy: string;
    candidate: string;
    manager: string;
    date: string;
    status: string;
  };